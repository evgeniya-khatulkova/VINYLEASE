<div class='row justify-content-center'>
  <div class='col-6' style="color: #425936">
    <h1>Add record </h1>
    <h4>Let the record spin...</h4>
      <%= render "form", record: @record %>
      <br>
      <%= link_to "Back", root_path, class: "btn btn-outline-secondary rounded-pill" %>
  </div>
</div>

