<div class="new-review">
<div class="review-input" style="color: #425936">
<%= simple_form_for [@reservation, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Submit review", class: "btn btn-outline-secondary rounded-pill" %>
 <% end %>

<%= link_to "All vinyls", records_path(@record), class: "btn btn-outline-secondary rounded-pill"%>
</div>
</div>
