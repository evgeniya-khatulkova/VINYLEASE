<div class="navbar fixed-top navbar-expand-sm navbar-light navbar-lewagon">
  <div class="container-fluid">
    <div class="logo">
    <%= link_to root_path, class: "navbar-brand" do %>
      <%= image_tag "icons8-record-player-80 (1).png"%>
    <% end %>
    <h2>VINYLEASE</h2>
    </div>

    <%# <%= form_with url: root_path, method: :get, class="d-flex" %>
        <%# <input class="form-control me-2 rounded-pill" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-secondary rounded-pill" type="submit">Search</button>
    </form> %>
    <% if user_signed_in? %>
    <%= form_with url: root_path, method: :get, class: "d-flex" do %>
      <%= text_field_tag :query,
          params[:query],
          class: "form-control",
          placeholder: "Find a record"
      %>
      <%= submit_tag "Search", class: "btn btn-outline-secondary rounded-pill" %>
    <% end %>


    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
          <li class="nav-item active">
            <%= link_to "My reservations", reservations_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Add record", new_record_path, class: "nav-link" %>
          </li>
          <li class="nav-item dropdown">
            <%= image_tag "https://source.unsplash.com/random/240x320/?face", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { bs_toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%= link_to "Log out", destroy_user_session_path, class: "dropdown-item", data: {turbo_method: :delete} %>
            </div>
          </li>
        <% else %>
          <li class="nav-item" id="log-in">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li-->
        <% end %>
      </ul>
    </div>
  </div>
</div>
